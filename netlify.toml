[build]
  command = "bun build ./index.html ./games/*/index.html --outdir=dist --minify"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
